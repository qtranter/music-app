package com.audiomack.utils

import android.content.Context
import okhttp3.OkHttpClient

object StethoUtils {
    fun initStetho(context: Context) = Unit
    fun addNetworkInterceptor(builder: OkHttpClient.Builder) = Unit
}
